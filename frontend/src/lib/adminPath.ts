/**
 * Base path for the admin panel. Change this one value to relocate the entire
 * admin area (routes, nav links, redirects all derive from it).
 */
export const ADMIN_BASE = "/cipanel";
export const ADMIN_LOGIN = `${ADMIN_BASE}/login`;
